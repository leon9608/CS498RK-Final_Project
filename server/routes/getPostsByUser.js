var express = require('express'),
    router = express.Router(),
    posts = require('../models/post'),
    mongoose = require('mongoose');

//Get, api/getPostsByUser/:id

//TODO: need to have user implement first
module.exports = router;
